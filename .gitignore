# build output
build/
*.exe
*.obj
*.lib
*.exp
*.pdb
*.ilk

# Visual Studio
.vs/
*.vcxproj.user

# model weights — download separately (see README)
stories15M.bin

# tooling / local
.claude/

# CUDA port — held back for a follow-up post (kept locally + in git history)
run_cuda.cu
gpu_check.cu
gpu_matmul.cu
